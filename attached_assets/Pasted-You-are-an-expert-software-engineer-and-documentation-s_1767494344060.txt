You are an expert software engineer and documentation specialist with deep knowledge of Replit projects, web/app development, and flag football domain specifics (e.g., rules, scoring, team management, gameplay mechanics). Your task is to generate a "Comprehensive Project Context Document" for my flag football app built in Replit. This document will serve as a master file to onboard other LLMs or developers, capturing the full current state so they can help continue building from here without starting from scratch.To generate this document, analyze the entire Replit project, including all files, code, dependencies, and configurations. If you need more details, assume based on standard practices for a flag football app (e.g., user authentication, game scheduling, stats tracking, real-time updates). [Optional: Insert any exported code, file listings, or additional project notes here if not automatically accessible, e.g., "Main files: app.py (core logic), database.py (SQLite integration), frontend.html (UI), etc."]Structure the output document strictly in Markdown format for readability, with the following sections. Make it concise yet thorough—aim for 1500-3000 words, focusing on facts without fluff. Use bullet points, code snippets, and diagrams (in text form, like Mermaid or ASCII) where helpful.Project Overview: High-level description, goals, target users (e.g., players, coaches, leagues), key features implemented so far (e.g., team creation, game simulation, scorekeeping), tech stack (e.g., Python/Flask backend, HTML/JS frontend, Replit database), and any assumptions or constraints.
Architecture and Design: System diagram (text-based), data flow, MVC or other patterns used, key modules/components, and how they interact (e.g., API endpoints, event handlers).
File Structure: Tree view of all files/folders in the Replit project, with brief descriptions of each (e.g., purpose, key contents).
Code Breakdown: Summarize core elements:Classes/objects and their methods/attributes.
Key functions/scripts, with pseudocode or snippets for critical logic (e.g., flag pull detection, win conditions).
Databases/schemas (e.g., tables for users, teams, games).
UI/UX components (e.g., pages, forms, real-time elements via WebSockets).
Integrations (e.g., external APIs for weather in outdoor games, auth libraries).
Dependencies and Setup: List all packages/libraries (e.g., via requirements.txt), environment variables, Replit-specific configs (e.g., run commands, ports), and installation/run instructions.
Current State and Progress: What's working, known bugs/issues, completed milestones, pending features (e.g., multiplayer mode, analytics dashboard), and any technical debt or refactor opportunities.
Domain-Specific Details: Flag football rules integrated (e.g., 7v7 format, no-contact mechanics), custom logic (e.g., scoring algorithms, player stats formulas), and how the app handles edge cases.
Guidelines for Continuation: Best practices for contributing (e.g., coding style, testing), how to extend features, and prompts/examples for using this document with other LLMs (e.g., "Based on this context, suggest improvements to the scoring system.").
Version and Metadata: Current version (e.g., v0.5), last updated date, author notes, and any links (e.g., Replit project URL).

Output ONLY the generated Markdown document, titled "Flag Football App Project Context Document". Do not add any extra commentary outside the document.

